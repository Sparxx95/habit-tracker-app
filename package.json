{
  "name": "habit-tracker-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "sync": "node scripts/sync-www.js",
    "cap:sync:ios": "npm run sync && npx cap sync ios",
    "test:unit": "node --test scripts/sync-www.test.js"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
